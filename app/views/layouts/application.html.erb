<!DOCTYPE html>
<html>
  <head>
    <title>BurningAirlines</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>
  <body>
    <nav>
    <header>
       <%# Link to login, singup, edit and delete functions %>
      <div class="login">
          <% if @current_user.present? %>
            Welcome, <%= @current_user.name %>
            |2
            <%= link_to 'LogOut', login_path, method:'delete'%>
            |
            <%= link_to 'Edit Account', edit_user_path(@current_user.id)%>
            |
            <%= link_to 'Profile', user_path(@current_user.id) %> 
            |
            <%# Pop-up message warning users to confirm deleting a profile before proceeding %>
            <%= link_to 'Delete Account', user_path(@current_user.id), method: 'delete', :data => { :confirm => 'Are you sure you want to Delete your account?'} %>
            |
            <%= link_to 'Create New Planes', user_path(@current_user.id) %>
            <% else %>
            <%= link_to 'Login', login_path %>
            |
            <%= link_to 'Sign Up', new_user_path %>
          <% end %>
        </div>
    </header>
    <br>
      <h1 class="page-header">MJE AIRWAYS!</h1>
      <p>We only have little seats as we are exclusive</p>


        <div class="nav">
          <%= link_to 'Home', root_path %>
          |
          |
        </div>
    </nav>

    <%= yield %>

    <footer>
    <hr>
    &copy; Secure & Private Blockchain for NFT's
    </footer>
  </body>
</html>
